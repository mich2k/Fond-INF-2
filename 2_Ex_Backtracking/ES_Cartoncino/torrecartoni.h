#ifndef TORRE_CARTONI_H
#define TORRE_CARTONI_H

#include <stdlib.h>
#include <stdint.h>
#include <stdio.h>
#include <stdbool.h>
#include <string.h>

typedef struct {
    unsigned p; // Peso
    unsigned a; // Altezza
    unsigned l; // Limite
} cartone;


/*uint8_t standard(const uint16_t*, uint16_t*, uint8_t);
bool check_double(const uint16_t*, uint8_t);
bool isvalid(const cartone*, const uint16_t*, uint8_t);
void BactrackSubset(const cartone*, const int, int, uint16_t*, uint8_t, uint16_t, uint16_t* , uint16_t* , unsigned int* );
void TorreCartoni(cartone*, int);*/

#endif // TORRE_CARTONI_H
